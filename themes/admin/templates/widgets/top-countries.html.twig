<div class="mb-5 {{ conf.cols_top_countries }}">
    <h5 class=" mb-2">
        {% if hide_show_all is not defined %}
            <a href="{{ pageStats.urls.topCountries }}" title="{{ "PLUGIN_PAGE_INSIGHTS.VIEW_ALL"|t }}">
               <i class="fa fa-fw fa-line-chart"></i>
               {{ "PLUGIN_PAGE_INSIGHTS.TOP_COUNTRIES"|t }}</a>
        {% else %}
            {{ "PLUGIN_PAGE_INSIGHTS.TOP_COUNTRIES"|t }}
        {% endif %}
    </h5>

    {% set conf = config.plugins['page-insights'] %}
    {% if conf.style_top_countries == 'table' %}
        {% include 'widgets/top-countries-table.html.twig' %}
    {% else %}
        {% include 'widgets/top-countries-pie.html.twig' %}
    {% endif %}

    {% include 'widgets/geo-db-status.html.twig' %}
</div>
